# Informe diario — 2026-05-19

## Objetivos del día
- Integrar base de datos y endpoints de auth (registro/login).
- Añadir protecciones contra SQLi/XSS y almacenar JWT en app móvil.

## Cambios realizados
- Backend: creada tabla `users`, endpoints `/auth/register` y `/auth/login` con `bcrypt`, queries parametrizadas y `xss` sanitización.
- Backend: añadido `helmet` y `CORS`, y script de inicialización de BD.
- Móvil: integrado flujo de login/registro y guardado seguro de JWT (`expo-secure-store`).
- Seguridad: añadido overrides para `postcss` en `package.json` para mitigar vulnerabilidades transitivas.

## Bloqueadores
- Algunas vulnerabilidades moderate en dependencias de Expo requieren actualización mayor de `expo`.

## Consejos / Próximos pasos
- Crear rama `fix/security-updates` y probar `npm audit fix --force` en rama de pruebas.
- Implementar listado de usuarios protegido por JWT para ver registros.
- Monitorizar dependencias con Dependabot o Snyk.

## Notas rápidas
- API base usada en móvil: `http://10.0.2.2:3000` (ajustar según entorno).
